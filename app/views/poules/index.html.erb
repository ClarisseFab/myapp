<h1>Poules</h1>
<br>
<table class="table table-bordered table-hover table-striped">
  <thead class="table-danger">
    <tr>
      <th scope="col">Nom poule</th>
      <% @poules.first.equipes.each_with_index do |poule, index| %>
      <th scope="col">Equipe <%= index+1 %></th>
      <% end %> 
    </tr>
  </thead>
  <tbody>
    <% @poules.each do |poule| %>
      <tr>
        <th><%= poule.nom_poule %></th>
       <% poule.equipes.each do |equipe| %> 
        <th><%= equipe.nom_equipe %></th>
       <% end %> 
      </tr>
    <% end %> 
  </tbody>
</table>
<br>
<%= link_to "Changer les poules", generate_poules_path, class:"btn btn-secondary" %>


